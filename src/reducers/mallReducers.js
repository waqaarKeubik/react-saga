let initialState = {
  malls: []
};
export default function User (state = initialState, action = {}){
  switch(action.type){

    // case LOGGED_IN_USER:
    //   return state;
    default:
      return state;
  }
}
